apiVersion: v1

kind: Service

metadata:
  name: notes-service
  namespace: notes-app

spec:

  type: NodePort

  selector:
    app: notes-app

  ports:

  - port: 8080

    targetPort: 8080

    nodePort: 30080